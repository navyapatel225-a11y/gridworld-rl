[project]
name = "gridworld-rl"
version = "0.1.0"
description = "GridWorld RL Environment"
authors = [
    {name = "Navya Patel"}
]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[project.scripts]
server = "server.app:app"